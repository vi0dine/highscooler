%article.media.opinion
  %figure.media-left
    %span.icon.is-large
      %icon{class: "fas fa-#{icon} fa-3x opinion_icon"}
  .media-content
    %strong= o.user.username + '  |  ' + (o.user&.academy&.name.nil? ? 'Brak informacji' : o.user.academy.name)
    %br
    %p= o.body