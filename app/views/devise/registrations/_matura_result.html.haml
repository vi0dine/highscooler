.field.is-grouped.has-addons
  .control.has-icons-left
    .select
      = f.select(:subject, [['Biologia', 'biology'], ['Matematyka', 'math'], ['Chemia', 'chemistry']]) ## Should fetch from db
      %span.icon.is-small.is-left
        %i.fas.fa-chalkboard-teacher
  .control
    .select
      = f.select(:level, [['Podstawowy', 'basic'], ['Rozszerzony', 'advanced']])
  .control
    = f.number_field :result, class: 'input'