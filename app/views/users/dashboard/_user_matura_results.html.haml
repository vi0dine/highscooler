%table.table.is-fullwidth
  %thead
    %tr
      %th Przedmiot
      %th Poziom
      %th Wynik
  %tbody
  - @user.matura_results.each do |mr|
    %tr
      %td 
        = mr.matura_subject.name
      %td
        = MaturaResult.human_enum_name(:level, mr.level)
      %td
        = mr.result
.button.is-primary{'v-on:click':'showAddResultForm()'} Dodaj wynik
= form_for(@matura_result, url: add_matura_result_path(@matura_result, id: current_user.id), method: :post, authenticity_token: true) do |f|
  = render 'add_matura_results_form', f: f