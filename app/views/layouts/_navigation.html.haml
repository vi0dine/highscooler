%nav.navbar.is-transparent{:role => "navigation"}
  .container.is-fluid
    .navbar-brand
      = link_to image_tag("logo_xs.png", size: '110x51', style: 'padding: 5px'), root_path
    .navbar-menu
      .navbar-end
        = link_to 'Aktualności', '#', :class => 'navbar-item'
        = link_to 'Uczelnie', academies_path , :class => 'navbar-item'
        = link_to 'Kierunki', fields_path , :class => 'navbar-item'
        - if user_signed_in?
          = link_to 'Wyloguj', destroy_user_session_path, :class => 'navbar-item'
        - else
          = link_to 'Zaloguj', new_user_session_path, :class => 'navbar-item'
          .buttons
            = link_to 'Zarejestruj', new_user_registration_path, method: :get, :class => 'button is-primary'
